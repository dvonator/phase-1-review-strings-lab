// Write your code in this file!

const currentUser = "David Vo";

const welcomeMessage = `Welcome to Flatbook, ${currentUser}!`;

const excitedWelcomeMessage = `WELCOME TO FLATBOOK, ${currentUser.toUpperCase()}!`; // remember to add parenthesis after the .touppercase etc etc

const shortGreeting = `Welcome, ${currentUser.slice(0, 1)}!`;
